require("dotenv").config();

module.exports = {
    TOKEN: process.env.TOKEN || "TOKEN GİR",  // bot token gir :)
    PREFIX: process.env.PREFIX || "u.", // bot prefix
    SAHİP: process.env.SAHİP || "1017079348269953060" , //sahip id :()
}